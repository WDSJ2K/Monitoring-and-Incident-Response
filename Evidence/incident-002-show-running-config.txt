========================================================
=== POST-RELOAD CONFIGURATION CHECK ===
========================================================

Router>enable
Router#show running-config

Current configuration: 703 bytes

Key Output:
- Hostname: Router
- Interface GigabitEthernet0/0:
  - IP address: 10.10.10.1/24
  - Duplex: auto
  - Speed: auto

--------------------------------------------------------

Observation:
- Router booted with default hostname "Router"
- Configuration size present, but startup-config hostname (WDSJ2K) not applied

Implication:
- Confirms device booted without loading expected startup-config
- Consistent with misconfigured config-register behavior

========================================================
END CONFIGURATION VERIFICATION
========================================================

