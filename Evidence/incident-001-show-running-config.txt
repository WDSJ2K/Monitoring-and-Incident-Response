========================================================
=== POST-RELOAD RUNNING CONFIG (RouterXxX) ===
========================================================

RouterXxX#show running-config

Current configuration: 744 bytes

Key Observations:
- Hostname: RouterXxX  
- No interfaces with IP addresses configured  
- All GigabitEthernet interfaces are administratively down  
- VLAN 1 exists but is shutdown  
- Static host entries present:
  - server1 → 1.1.1.1  
  - server2 → 2.2.2.2  

--------------------------------------------------------

Interpretation:
- This configuration is NOT the expected startup-config.
- Device booted into a different baseline configuration.
- Confirms startup-config was ignored at boot.

========================================================
END CONFIGURATION CHECK
========================================================
